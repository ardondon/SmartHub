# SmartHub 开发路线图 - 技术实施计划

> 🚀 **最新状态** (2025-11-24): 正在开发极简MVP版本！
> 
> **当前执行策略**: 先做一个可运行的最小版本，验证技术可行性
> - ✅ 只支持腾讯连连平台
> - ✅ 实现基本设备控制（开关、状态查看）
> - ✅ Jetpack Compose UI + MVVM架构
> - ✅ 项目框架已搭建（2025-11-24）
> 
> 完成极简MVP后，再按照下方完整计划逐步扩展功能。

---

> 📋 **完整开发计划**: 以下为原定18个月的详细规划，供参考

**项目**: SmartHub Android多云智能网关  
**文档版本**: v1.1 (更新: 2025-11-24)  
**原计划编写日期**: 2025年11月  
**实际启动时间**: 2025年11月24日 ✅  
**计划周期**: 18个月 (从MVP到市场成熟)  
**当前状态**: � 极简MVP开发中

---

## 📋 项目概览

### 技术栈

```
核心架构: 纯本地Android应用
├─ 语言: Kotlin
├─ UI: Jetpack Compose
├─ 架构: MVVM + Clean Architecture
└─ 异步: Coroutines + Flow

数据存储 (本地):
├─ 数据库: Room + SQLite
├─ 配置: DataStore (替代SharedPreferences)
└─ 缓存: 内存缓存 + 持久化

云平台集成 (直连):
├─ 腾讯连连: IoT Android SDK → 腾讯云
├─ 天猫精灵: 生活物联网SDK → 阿里云
├─ 小米IoT: MIoT SDK → 小米云
├─ 华为HiLink: HiLink SDK → 华为云
└─ 更多平台插件...

网络层:
├─ MQTT: Eclipse Paho (设备通信)
├─ HTTP: OkHttp + Retrofit (云平台API)
└─ WebSocket: OkHttp (实时通信)

硬件平台:
├─ 系统: Android 8.0+
├─ 设备: 二手平板 (小米/华为/联想等)
├─ 屏幕: 8-11寸触摸屏
└─ 网络: WiFi + 4G/5G (可选)

设计理念:
✅ 本地优先 - 所有核心功能本地运行
✅ 离线可用 - 无网络也能控制本地设备
✅ 隐私安全 - 数据不上传到自建服务器
✅ 直连云平台 - 使用官方SDK，无中间层
```

### 开发团队配置

```
Phase 1 (0-6月): 5人核心团队 - MVP开发
├─ Android高级工程师: 2人 (核心开发)
├─ 云平台集成工程师: 1人 (多平台SDK对接)
├─ UI/UX设计师: 1人 (界面设计)
└─ 产品经理/测试: 1人 (需求+质量把控)

Phase 2 (6-12月): 10人 - 功能完善
├─ Android开发: 4人
│   ├─ 核心框架: 2人
│   └─ 功能模块: 2人
├─ 云平台集成: 2人 (扩展更多平台)
├─ UI/UX设计: 2人
├─ 测试工程师: 1人
└─ 产品/运营: 1人

Phase 3 (12-18月): 15人 - 规模化
├─ Android开发: 6人
├─ 云平台集成: 2人
├─ UI/UX设计: 2人
├─ 测试: 2人
├─ 产品: 2人
└─ 运营/客服: 1人

注: 无需后端开发人员，核心功能完全本地化
商业化功能(插件商店/企业管理)待Phase 3评估后决定
```

---

## 🏗️ 架构设计原则

### 为什么选择纯本地架构？

#### 核心优势
```
1. ✅ 成本优势
   - 无服务器运维成本 (节省¥5-20万/年)
   - 无带宽流量费用
   - 专注核心产品开发

2. ✅ 隐私安全
   - 用户数据完全本地存储
   - 不经过第三方服务器
   - 符合隐私保护趋势

3. ✅ 离线可用
   - 无网络也能控制本地设备
   - 场景执行不依赖云端
   - 稳定性更高

4. ✅ 开发简单
   - 减少50%开发工作量
   - 无需前后端协作
   - 更快的迭代速度

5. ✅ 用户体验
   - 响应速度更快 (无网络延迟)
   - 无需注册账号即可使用
   - 数据完全自主控制
```

#### 技术架构图
```
┌─────────────────────────────────────┐
│     SmartHub Android应用            │
│     (运行在二手平板上)              │
├─────────────────────────────────────┤
│  UI层 (Jetpack Compose)             │
├─────────────────────────────────────┤
│  业务层 (ViewModel + UseCase)       │
├─────────────────────────────────────┤
│  数据层                             │
│  ├─ Room数据库 (设备/场景/配置)    │
│  ├─ DataStore (用户偏好)            │
│  └─ 内存缓存                        │
├─────────────────────────────────────┤
│  云平台插件层                       │
│  ├─ 腾讯连连插件 ──→ 腾讯云API     │
│  ├─ 天猫精灵插件 ──→ 阿里云API     │
│  ├─ 小米IoT插件  ──→ 小米云API     │
│  └─ 华为HiLink插件 ──→ 华为云API   │
├─────────────────────────────────────┤
│  本地设备控制层                     │
│  ├─ MQTT客户端 (本地设备通信)      │
│  ├─ BLE扫描 (蓝牙设备)              │
│  └─ HTTP服务器 (配网页面)          │
└─────────────────────────────────────┘
         ↓           ↓           ↓
    [WiFi设备]  [BLE设备]  [云端设备]
```

### 商业化功能规划（后期可选）

#### Phase 3 评估项（12-18月后）
```
如果用户需求强烈，可考虑:

1. 云端账号系统 (可选)
   - 目的: 多设备同步配置
   - 方案: Firebase Auth (免费额度充足)
   - 成本: 几乎为0
   - 优先级: 低

2. 插件商店后台
   - 目的: 第三方插件分发
   - 方案: 静态文件托管 + CDN
   - 成本: ¥500-2,000/月
   - 优先级: 中

3. 企业版云端管理
   - 目的: 批量网关集中管理
   - 方案: 轻量级Web后台
   - 成本: ¥5,000-10,000/月
   - 优先级: 中 (仅企业版)

4. 远程访问服务
   - 目的: 外网访问家中网关
   - 方案: MQTT Broker中转 或 P2P打洞
   - 成本: ¥2,000-5,000/月
   - 优先级: 低
```

#### 决策原则
```
✅ 先验证市场需求
✅ 计算ROI (投入产出比)
✅ 评估用户付费意愿
✅ 考虑开发维护成本

保持灵活，按需扩展
```

---

## 🎯 开发阶段规划

---

## 🚀 Phase 0: 极简MVP (当前执行中 - 2025年11月)

> **执行策略**: 快速验证技术可行性，2-4周完成最小可用版本

### 目标
**构建一个可运行的最小版本，验证核心技术路线**

**里程碑**: 
- ✅ 项目框架搭建完成 (2025-11-24)
- 🔄 腾讯连连SDK集成
- 🔄 基础设备控制实现
- 📅 可演示版本 (预计2周内)

### 极简范围定义

```
只做最核心的功能:
✅ 单一平台: 只支持腾讯连连
✅ 基础UI: 设备列表 + 控制页
✅ 核心功能: 开关控制、状态查看
❌ 不做: 场景联动、多平台、数据统计
```

### Week 1: 框架搭建 ✅ (当前周)

**已完成**:
```
✅ Gradle项目配置
✅ 依赖管理 (Compose, Room, Hilt, MQTT)
✅ 项目结构设计
```

**本周计划**:
```kotlin
// 1. 创建核心架构
app/
├── data/
│   ├── local/              // Room数据库
│   │   ├── entity/         // Device实体
│   │   └── dao/            // DeviceDao
│   └── repository/         // DeviceRepository
├── domain/
│   ├── model/              // Device领域模型
│   └── usecase/            // GetDevicesUseCase, ControlDeviceUseCase
├── presentation/
│   ├── devices/            // 设备列表页
│   │   ├── DevicesScreen.kt
│   │   └── DevicesViewModel.kt
│   └── theme/              // Material 3主题
└── di/                     // Hilt模块

任务清单:
□ 创建项目包结构
□ 配置AndroidManifest.xml
□ 实现Application类 (Hilt)
□ 创建Material 3主题
□ 搭建导航框架
□ 创建数据库Schema
```

### Week 2: 腾讯连连集成

```kotlin
任务:
□ 研究腾讯云IoT Explorer Android SDK
□ 实现TencentIoTService
□ 设备发现与注册
□ MQTT连接管理
□ 属性上报/下发

核心类:
class TencentIoTService {
    fun connect(config: TencentConfig): Result<Unit>
    fun discoverDevices(): Flow<List<Device>>
    fun controlDevice(deviceId: String, action: DeviceAction)
    fun observeDeviceStatus(deviceId: String): Flow<DeviceStatus>
}
```

### Week 3: UI实现

```kotlin
功能:
□ 设备列表展示
□ 设备卡片组件
□ 设备控制界面
□ 加载/错误状态处理

界面:
1. DevicesScreen - 设备列表
   - LazyColumn显示所有设备
   - 设备卡片显示名称、状态、类型
   - 点击卡片进入详情

2. DeviceDetailScreen - 设备详情
   - 显示设备信息
   - 开关控制按钮
   - 实时状态更新
```

### Week 4: 测试与优化

```
□ 单元测试 (核心业务逻辑)
□ UI测试 (Compose测试)
□ 真实设备测试
□ Bug修复
□ 性能优化
□ 代码审查
□ 文档更新

产出:
- 可运行的APK
- 演示视频
- 使用文档
```

### 极简MVP技术栈

```
最小依赖集:
✅ Kotlin + Jetpack Compose
✅ MVVM架构
✅ Room (本地存储)
✅ Hilt (依赖注入)
✅ Coroutines + Flow
✅ 腾讯云IoT SDK
✅ MQTT

不包含:
❌ 多平台插件架构 (暂缓)
❌ DataStore (用Room替代)
❌ 复杂场景引擎
❌ 数据可视化
```

### 成功标准

```
✅ 能成功连接腾讯连连账号
✅ 能显示账号下的所有设备
✅ 能控制设备开关
✅ 设备状态能实时更新
✅ 应用稳定无崩溃
✅ 代码结构清晰可扩展
```

### 极简MVP完成后

如果验证成功，进入下方完整的Phase 1开发：
- 扩展支持天猫精灵、小米IoT
- 实现插件架构
- 添加场景联动
- UI/UX优化

---

## Phase 1: MVP开发 (预计开始: 2026 Q1, 周期: 6个月)

> 📅 **时间说明**: 以下时间线为相对时间，实际日期取决于项目启动时间
> 
> ⚠️ **注意**: Phase 1将在完成极简MVP并验证成功后启动

### 目标
构建可用的最小化产品，验证核心概念

**里程碑**: 支持3个云平台，100台内测设备
**预计完成**: 2026 Q2

### Month 1-2: 基础框架

#### Week 1-2: 项目搭建
```
任务清单:
□ 创建Android项目结构
□ 配置依赖管理 (Gradle)
□ 设置CI/CD (GitHub Actions)
□ 搭建开发环境
□ 代码规范/Git工作流

技术选型确认:
✅ Kotlin + Jetpack Compose
✅ MVVM + Clean Architecture
✅ Room + DataStore
✅ Hilt依赖注入

产出:
- 项目骨架代码
- 开发文档
- 环境配置指南
```

#### Week 3-4: 核心架构
```kotlin
// 项目结构
app/
├── data/
│   ├── repository/        // 数据仓库
│   ├── local/            // 本地数据源
│   └── remote/           // 远程数据源
├── domain/
│   ├── model/            // 领域模型
│   ├── usecase/          // 业务用例
│   └── repository/       // 仓库接口
├── presentation/
│   ├── ui/               // UI组件
│   ├── viewmodel/        // ViewModel
│   └── navigation/       // 导航
└── di/                   // 依赖注入

任务:
□ 实现数据层架构
□ 定义领域模型
□ 搭建MVVM框架
□ 配置依赖注入
□ 数据库设计

产出:
- 核心架构代码
- 数据模型定义
- API接口设计文档
```

#### Week 5-6: 基础功能
```
功能模块:
□ 用户认证系统
□ 配置管理
□ 本地数据持久化
□ 网络请求封装
□ 日志系统

技术实现:
// 配置管理
class ConfigManager @Inject constructor(
    private val dataStore: DataStore<Preferences>
) {
    suspend fun saveConfig(config: GatewayConfig)
    fun getConfig(): Flow<GatewayConfig>
}

// 日志系统
object Logger {
    fun d(tag: String, message: String)
    fun e(tag: String, error: Throwable)
}

产出:
- 配置管理模块
- 日志系统
- 本地存储方案
```

#### Week 7-8: UI框架
```
界面开发:
□ Material 3主题配置
□ 导航架构
□ 主界面框架
□ 通用组件库

Compose组件:
@Composable
fun GatewayScaffold(
    topBar: @Composable () -> Unit,
    bottomBar: @Composable () -> Unit,
    content: @Composable () -> Unit
)

@Composable
fun DeviceCard(device: Device, onClick: () -> Unit)

@Composable
fun SceneButton(scene: Scene, onExecute: () -> Unit)

产出:
- UI设计系统
- 通用组件库
- 主界面原型
```

### Month 3-4: 云平台集成

#### Week 9-10: 腾讯连连集成
```
任务:
□ 集成腾讯云IoT Android SDK
□ 实现MQTT连接
□ 设备注册/绑定
□ 属性上报/下发
□ 事件处理

代码实现:
class TencentCloudPlugin : CloudPlatformPlugin {
    override suspend fun connect(config: PlatformConfig): Result<Unit>
    override suspend fun registerDevice(device: Device): Result<String>
    override suspend fun reportProperty(deviceId: String, properties: Map<String, Any>)
    override fun setPropertyCallback(callback: PropertyCallback)
}

测试:
□ 单元测试
□ 集成测试
□ 真实设备测试

产出:
- 腾讯连连插件完整实现
- 单元测试覆盖率>80%
- 集成文档
```

#### Week 11-12: 天猫精灵集成
```
任务:
□ 集成阿里生活物联网SDK
□ Alink协议适配
□ 设备管理
□ 数据上报

实现要点:
- Alink JSON格式转换
- 物模型适配
- 错误处理

产出:
- 天猫精灵插件
- 测试报告
```

#### Week 13-14: 小米IoT集成
```
任务:
□ MIoT Spec协议研究
□ 小米IoT SDK集成
□ SIID/PIID映射
□ 设备控制

挑战:
⚠️ 小米开放度较低
⚠️ 需要逆向研究

产出:
- 小米IoT插件(基础版)
- 技术难点文档
```

#### Week 15-16: 插件架构完善
```
任务:
□ 统一插件接口
□ 插件管理器
□ 插件热加载
□ 多平台协同

架构设计:
interface CloudPlatformPlugin {
    val platformName: String
    val platformId: String
    suspend fun connect(config: PlatformConfig): Result<Unit>
    suspend fun disconnect()
    // ... 其他接口
}

class PluginManager {
    fun registerPlugin(plugin: CloudPlatformPlugin)
    fun getPlugin(platformId: String): CloudPlatformPlugin?
    suspend fun connectAll()
}

产出:
- 完整插件架构
- 3个云平台插件
- 插件开发文档
```

### Month 5-6: 核心功能完善

#### Week 17-18: 设备管理
```
功能:
□ 设备列表
□ 设备详情
□ 设备控制
□ 设备状态同步
□ 多平台设备聚合

UI实现:
@Composable
fun DeviceListScreen(viewModel: DeviceViewModel) {
    LazyColumn {
        items(devices) { device ->
            DeviceCard(
                device = device,
                onClick = { navigateToDetail(device) },
                onControl = { action -> viewModel.controlDevice(device, action) }
            )
        }
    }
}

产出:
- 设备管理完整功能
- UI/UX优化
```

#### Week 19-20: 场景联动
```
功能:
□ 场景创建
□ 条件配置
□ 动作配置
□ 场景执行
□ 跨平台联动

场景引擎:
class SceneEngine {
    fun createScene(scene: Scene): Result<String>
    fun executeScene(sceneId: String): Result<Unit>
    fun evaluateConditions(scene: Scene): Boolean
}

// 场景示例
Scene(
    name = "回家模式",
    triggers = listOf(
        Trigger.Location(lat = xx, lon = xx, radius = 500)
    ),
    conditions = listOf(
        Condition.Time(after = "18:00")
    ),
    actions = listOf(
        Action.TencentDevice(deviceId = "xxx", action = "power_on"),
        Action.TmallDevice(deviceId = "yyy", action = "set_temp", params = mapOf("value" to 26))
    )
)

产出:
- 场景引擎
- 场景编辑器UI
```

#### Week 21-22: 数据可视化
```
功能:
□ 设备状态统计
□ 能耗分析
□ 使用趋势
□ 图表展示

使用图表库:
implementation "com.github.PhilJay:MPAndroidChart:v3.1.0"

@Composable
fun EnergyDashboard(data: EnergyData) {
    Column {
        PieChart(data = data.breakdown)
        LineChart(data = data.trend)
        BarChart(data = data.comparison)
    }
}

产出:
- 数据统计模块
- 可视化界面
```

#### Week 23-24: 测试与优化
```
任务:
□ 功能测试
□ 性能测试
□ UI/UX优化
□ Bug修复
□ 文档完善

测试内容:
- 单元测试覆盖率>80%
- 集成测试
- 压力测试(100+设备)
- 长期稳定性测试(7×24小时)

产出:
- MVP版本v0.1.0
- 测试报告
- 用户手册
```

---

## Phase 2: 公测与优化 (预计: 2025 Q3-Q4, 周期: 6个月)

### 目标
完善功能，扩展平台，准备商业化

**里程碑**: 10个云平台，5,000用户，稳定运行
**预计完成**: 2025 Q4

### Month 7-8: 平台扩展

#### 更多云平台集成
```
优先级顺序:
1. 华为HiLink (Week 25-26)
2. 百度智能云 (Week 27-28)
3. 京东小京鱼 (Week 29-30)
4. 涂鸦智能 (Week 31-32)

每个平台2周开发周期:
Week 1: SDK集成 + 基础功能
Week 2: 测试 + 文档

产出:
- 7个云平台插件
- 平台兼容性矩阵
```

#### 国际平台(可选)
```
AWS IoT Core
Google Home
Amazon Alexa

产出:
- 国际化准备
- 多语言支持
```

### Month 9-10: 功能增强

#### 高级自动化
```
功能:
□ AI场景推荐
□ 学习型自动化
□ 复杂条件组合
□ 定时任务
□ 地理围栏

AI推荐引擎:
class AISceneRecommender {
    fun analyzeUserBehavior(history: List<Action>): List<Scene>
    fun recommendScenes(): List<RecommendedScene>
}

产出:
- AI推荐系统
- 高级自动化引擎
```

#### 多网关协同
```
功能:
□ 多网关发现
□ 数据同步
□ 负载均衡
□ 故障切换

架构:
- P2P通信
- 分布式状态同步
- 冲突解决机制

产出:
- 多网关协同方案
- 企业版基础
```

### Month 11-12: 商业化准备

#### 订阅系统
```
功能:
□ 订阅管理
□ 支付集成
□ 权限控制
□ 试用/激活

技术:
- Google Play Billing
- 自建订阅系统
- 权限验证

产出:
- 完整订阅系统
- 支付流程
```

#### 插件商店
```
功能:
□ 插件浏览
□ 插件安装
□ 版本管理
□ 评价系统

架构:
- 插件仓库后端
- 插件签名验证
- 沙箱运行环境

产出:
- 插件商店v1.0
- 开发者文档
```

---

## Phase 3: 规模化 (预计: 2026 Q1-Q2, 周期: 6个月)

### 目标
市场扩张，生态建设

**里程碑**: 50,000用户，月销5,000台
**预计完成**: 2026 Q2

### Month 13-14: 性能优化

```
优化项:
□ 启动速度优化
□ 内存占用优化
□ 电池续航优化
□ 网络流量优化
□ 数据库性能优化

目标:
- 冷启动<3秒
- 内存占用<150MB
- 24小时待机电量消耗<15%
- 流量<10MB/天

技术手段:
- 启动优化: 延迟初始化、异步加载
- 内存优化: 对象池、缓存策略
- 电池优化: Doze模式适配、JobScheduler
- 流量优化: 数据压缩、增量同步
```

### Month 15-16: 企业功能

```
功能:
□ 企业管理后台
□ 批量设备管理
□ 权限分级管理
□ 操作日志审计
□ API接口开放

架构:
- Web管理后台(React)
- RESTful API
- OAuth2认证
- RBAC权限模型

产出:
- 企业版v1.0
- API文档
- 管理后台
```

### Month 17-18: 生态建设

```
开发者生态:
□ 插件开发SDK
□ API文档完善
□ 开发者社区
□ 技术支持

用户生态:
□ 场景分享社区
□ 用户论坛
□ 在线客服
□ 知识库

产出:
- 完整生态系统
- 活跃社区
```

---

## 🔧 技术实施细节

### 硬件适配

#### 平板选型标准
```
基础要求:
- Android 8.0+
- 3GB+ RAM
- 32GB+ 存储
- 5000mAh+ 电池
- WiFi + 蓝牙

推荐型号:
1. 小米平板4 (¥180, 8寸, 4GB/64GB)
2. 小米平板5 (¥500, 11寸, 6GB/128GB)
3. 华为MatePad (¥400, 10.4寸, 6GB/128GB)
4. 荣耀平板5 (¥150, 8寸, 3GB/32GB)

测试设备矩阵:
- 至少3个品牌
- 至少5个型号
- 覆盖高中低端
```

#### 硬件兼容性测试
```
测试项:
□ 屏幕适配(7-12寸)
□ 性能测试(多设备并发)
□ 电池续航
□ 散热测试
□ 传感器兼容性
□ 4G/5G网络

工具:
- Android Profiler
- Battery Historian
- Monkey/UI Automator
```

### Android作为网关的技术可行性

#### 为什么Android适合做智能家居网关？

**✅ 技术优势**
```
1. 成熟的生态系统
   - 丰富的开发工具和库
   - 完善的云平台SDK支持
   - 大量开源组件可用

2. 硬件成本优势
   - 二手平板价格低廉(¥150-500)
   - 供应链成熟，维修方便
   - 性能完全满足网关需求

3. 用户体验优势
   - 大屏触控界面
   - 即开即用，无需学习成本
   - 可视化配置和监控

4. 开发效率高
   - 单端开发(无需前后端分离)
   - 快速迭代
   - 易于调试和测试
```

#### 后台服务保活策略

**关键技术点**
```kotlin
// 1. 前台服务 - 最核心的保活机制
class GatewayService : Service() {
    override fun onStartCommand(intent: Intent?, flags: Int, startId: Int): Int {
        // 创建前台通知
        val notification = NotificationCompat.Builder(this, CHANNEL_ID)
            .setContentTitle("SmartHub正在运行")
            .setContentText("已连接${deviceCount}个设备")
            .setSmallIcon(R.drawable.ic_gateway)
            .setPriority(NotificationCompat.PRIORITY_LOW)
            .build()
        
        startForeground(NOTIFICATION_ID, notification)
        
        return START_STICKY // 被系统杀死后自动重启
    }
}

// 2. 屏幕常亮（低亮度模式）
class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // 保持屏幕常亮，规避Doze模式
        window.addFlags(WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)
        // 设置最低亮度节省电量
        val layoutParams = window.attributes
        layoutParams.screenBrightness = 0.01f // 1%亮度
        window.attributes = layoutParams
    }
}

// 3. 电池优化豁免
fun requestBatteryOptimizationExemption() {
    if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.M) {
        val intent = Intent()
        val pm = getSystemService(Context.POWER_SERVICE) as PowerManager
        if (!pm.isIgnoringBatteryOptimizations(packageName)) {
            intent.action = Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS
            intent.data = Uri.parse("package:$packageName")
            startActivity(intent)
        }
    }
}

// 4. WakeLock - 保持CPU唤醒
val wakeLock: PowerManager.WakeLock =
    (getSystemService(Context.POWER_SERVICE) as PowerManager).run {
        newWakeLock(PowerManager.PARTIAL_WAKE_LOCK, "SmartHub::GatewayLock").apply {
            acquire() // 仅在必要时使用
        }
    }

// 5. WorkManager - 定期检查服务状态
class ServiceCheckWorker(context: Context, params: WorkerParameters) 
    : Worker(context, params) {
    override fun doWork(): Result {
        // 检查服务是否运行，如未运行则重启
        if (!isServiceRunning()) {
            startGatewayService()
        }
        return Result.success()
    }
}
```

#### 长连接维护方案

**MQTT连接保活**
```kotlin
class MqttManager {
    private val mqttOptions = MqttConnectOptions().apply {
        // 1. 自动重连
        isAutomaticReconnect = true
        
        // 2. 清理会话
        isCleanSession = false
        
        // 3. 心跳间隔(秒)
        keepAliveInterval = 60
        
        // 4. 连接超时(秒)
        connectionTimeout = 30
        
        // 5. 遗嘱消息
        setWill("gateway/status", "offline".toByteArray(), 1, true)
    }
    
    // 监听连接状态
    override fun connectionLost(cause: Throwable?) {
        // 连接丢失时自动重连
        reconnectWithBackoff()
    }
    
    private fun reconnectWithBackoff() {
        scope.launch {
            var delay = 1000L
            while (!isConnected) {
                try {
                    connect()
                    break
                } catch (e: Exception) {
                    delay(delay)
                    delay = (delay * 2).coerceAtMost(60000L) // 最多60秒
                }
            }
        }
    }
}
```

#### Kiosk模式部署（可选）

**企业/家庭固定部署方案**
```kotlin
// 1. 设置为默认启动器
<intent-filter>
    <action android:name="android.intent.action.MAIN" />
    <category android:name="android.intent.category.HOME" />
    <category android:name="android.intent.category.DEFAULT" />
</intent-filter>

// 2. 锁定任务模式
if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.LOLLIPOP) {
    startLockTask()
}

// 3. 设备管理员模式
class GatewayDeviceAdminReceiver : DeviceAdminReceiver()

// 4. 隐藏系统UI
window.decorView.systemUiVisibility = (
    View.SYSTEM_UI_FLAG_LAYOUT_STABLE
    or View.SYSTEM_UI_FLAG_LAYOUT_HIDE_NAVIGATION
    or View.SYSTEM_UI_FLAG_LAYOUT_FULLSCREEN
    or View.SYSTEM_UI_FLAG_HIDE_NAVIGATION
    or View.SYSTEM_UI_FLAG_FULLSCREEN
    or View.SYSTEM_UI_FLAG_IMMERSIVE_STICKY
)
```

#### 成功案例参考

```
国内外类似产品:
✅ 小米智能家庭中枢 - Android平板方案
✅ 天猫精灵CC - Android系统
✅ Amazon Echo Show - 基于Fire OS (Android分支)
✅ Lenovo Smart Display - Android Things
✅ 华为智慧屏 - HarmonyOS (Android兼容)

关键经验:
1. 专用设备 + 前台服务 = 稳定运行
2. 屏幕常亮可有效规避Doze限制
3. 固定插电使用，无续航焦虑
4. 用户体验优于纯后台方案
```

#### 潜在限制与解决方案

**限制1: 系统更新可能杀进程**
```
解决方案:
- 监听系统启动广播(BOOT_COMPLETED)
- 自动重启服务
- WorkManager定期检查
```

**限制2: 内存不足时可能被杀**
```
解决方案:
- 前台服务优先级高
- 合理控制内存占用
- 及时释放不用的资源
```

**限制3: Doze模式限制后台网络**
```
解决方案:
- 屏幕常亮(低亮度)规避Doze
- 申请电池优化豁免
- FCM高优先级推送唤醒
```

**限制4: 部分厂商系统激进杀后台**
```
解决方案:
- 引导用户添加白名单
- 使用厂商SDK(小米、华为等)
- Kiosk模式锁定应用
```

#### 技术验证清单

```
开发前验证项:
□ 前台服务24小时稳定性测试
□ MQTT长连接保活测试
□ 不同厂商ROM兼容性测试
□ Doze模式下的表现测试
□ 内存/CPU占用长期监控
□ 断网重连机制测试
□ 系统重启后自启动测试
□ 多设备并发控制测试

目标指标:
✅ 7×24小时持续运行
✅ 断线重连时间<5秒
✅ 内存占用<200MB
✅ CPU占用(空闲)<3%
✅ 服务存活率>99.9%
```

### 软件架构

#### 模块划分
```
app/
├── core/                 # 核心框架
│   ├── network/         # 网络层
│   ├── database/        # 数据库
│   ├── preference/      # 配置存储
│   └── utils/           # 工具类
├── feature/             # 功能模块
│   ├── auth/           # 认证
│   ├── device/         # 设备管理
│   ├── scene/          # 场景
│   ├── dashboard/      # 仪表板
│   └── settings/       # 设置
├── platform/            # 云平台
│   ├── common/         # 通用接口
│   ├── tencent/        # 腾讯连连
│   ├── tmall/          # 天猫精灵
│   ├── xiaomi/         # 小米IoT
│   └── huawei/         # 华为HiLink
└── shared/              # 共享资源
    ├── ui/             # UI组件
    ├── model/          # 数据模型
    └── resources/      # 资源文件
```

#### 数据流架构
```
UI层 (Compose)
    ↓ User Action
ViewModel
    ↓ Use Case
Repository
    ↓ Data Source
[Local DB] ←→ [Remote API]
```

### 开发规范

#### 代码规范
```kotlin
// 命名规范
class DeviceManager           // PascalCase
fun connectDevice()           // camelCase
val MAX_DEVICES = 100         // UPPER_SNAKE_CASE

// 注释规范
/**
 * 设备管理器
 * 
 * @param context Android上下文
 * @param config 配置信息
 */
class DeviceManager(
    private val context: Context,
    private val config: Config
)

// 错误处理
sealed class Result<out T> {
    data class Success<T>(val data: T) : Result<T>()
    data class Error(val exception: Exception) : Result<Nothing>()
}
```

#### Git工作流
```
分支策略:
main          # 生产环境
├── develop   # 开发主线
├── feature/* # 功能分支
├── hotfix/*  # 紧急修复
└── release/* # 发布分支

提交规范:
feat: 新功能
fix: Bug修复
docs: 文档更新
refactor: 重构
test: 测试相关
chore: 构建/工具相关

示例:
feat(device): 添加设备批量控制功能
fix(mqtt): 修复断线重连问题
docs(api): 更新API文档
```

#### CI/CD流程
```yaml
# .github/workflows/android.yml
name: Android CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up JDK 17
        uses: actions/setup-java@v2
      - name: Build with Gradle
        run: ./gradlew build
      - name: Run tests
        run: ./gradlew test
      - name: Upload APK
        uses: actions/upload-artifact@v2
```

---

## 📊 质量保证

### 测试策略

#### 单元测试
```kotlin
@Test
fun `test device connection`() = runTest {
    val device = createTestDevice()
    val result = deviceManager.connect(device)
    assertTrue(result is Result.Success)
}

目标覆盖率:
- 核心功能: 90%+
- UI层: 60%+
- 整体: 80%+
```

#### 集成测试
```kotlin
@Test
fun `test full device control flow`() = runTest {
    // 1. 连接平台
    platformPlugin.connect(config)
    
    // 2. 注册设备
    val deviceId = platformPlugin.registerDevice(device)
    
    // 3. 控制设备
    val result = platformPlugin.controlDevice(deviceId, action)
    
    assertTrue(result.isSuccess)
}
```

#### UI测试
```kotlin
@Test
fun testDeviceListDisplay() {
    composeTestRule.setContent {
        DeviceListScreen()
    }
    
    composeTestRule
        .onNodeWithText("客厅灯")
        .assertIsDisplayed()
}
```

### 性能基准

```
启动性能:
- 冷启动: <3秒
- 热启动: <1秒
- 首屏渲染: <500ms

运行性能:
- 内存占用: <150MB (50个设备)
- CPU占用: <5% (空闲)
- 帧率: 60fps (UI操作)

网络性能:
- API响应: <500ms (P95)
- MQTT延迟: <200ms
- 重连时间: <3秒

电池性能:
- 24小时待机: <15%电量消耗
- 屏幕常亮: >8小时
```

---

## 📦 发布计划

### 版本规划

**v0.1.0 - MVP (Month 6)**
- 3个云平台
- 基础设备管理
- 简单场景
- 内测版

**v0.5.0 - 公测版 (Month 9)**
- 7个云平台
- 完整功能
- Play Store上线

**v1.0.0 - 正式版 (Month 12)**
- 10+平台
- 订阅系统
- 插件商店

**v1.5.0 - 企业版 (Month 15)**
- 企业功能
- API开放

**v2.0.0 - 生态版 (Month 18)**
- 完整生态
- 国际化

### 发布渠道

```
Android:
✅ Google Play Store
✅ 小米应用商店
✅ 华为应用市场
✅ 酷安
✅ GitHub Releases

分发策略:
- 免费版: 全渠道
- 付费功能: 内购/订阅
- 企业版: 直销+授权
```

---

## 💰 资源估算

### 开发成本

**Phase 1 (6个月, 预计2025年)**
```
人力: 5人 × 6月 × ¥25,000 = ¥75万
注: 基于2024年薪资水平估算，实际以启动时市场行情为准
设备: 测试设备 ¥5万
服务器: ¥2万
其他: ¥3万
合计: ¥85万
```

**Phase 2 (6个月)**
```
人力: 12人 × 6月 × ¥27,000 = ¥194万
设备: ¥8万
服务器: ¥5万
其他: ¥8万
合计: ¥215万
```

**Phase 3 (6个月)**
```
人力: 20人 × 6月 × ¥30,000 = ¥360万
设备: ¥10万
服务器: ¥10万
其他: ¥20万
合计: ¥400万
```

**总计: ¥700万 (18个月)**

> ⚠️ **成本说明**: 以上为初步估算，基于2024年底的市场行情。实际成本需在项目启动时重新评估。

---

## ⚠️ 风险管理

### 技术风险

**1. 平台API变更**
- 概率: 高
- 影响: 中
- 应对: 插件架构隔离，快速响应机制

**2. 兼容性问题**
- 概率: 中
- 影响: 中
- 应对: 广泛设备测试，兼容性层

**3. 性能瓶颈**
- 概率: 中
- 影响: 高
- 应对: 性能测试，优化迭代

### 进度风险

**关键路径**:
1. 云平台SDK集成 (Week 9-16)
2. 插件架构 (Week 15-16)
3. UI完善 (Week 17-24)

**Buffer时间**: 每个Phase预留20%缓冲

---

## 📈 成功指标

### 技术指标
- ✅ 代码覆盖率 >80%
- ✅ 崩溃率 <0.5%
- ✅ ANR率 <0.1%
- ✅ API成功率 >99%

### 产品指标
- ✅ 日活用户 (DAU)
- ✅ 月活用户 (MAU)
- ✅ 用户留存率 (D1/D7/D30)
- ✅ 付费转化率

### 业务指标
- ✅ 用户增长率
- ✅ 营收
- ✅ 用户满意度 (NPS)

---

---

## 📌 文档说明

本文档为**技术规划文档**，目的是：
- ✅ 明确技术选型和架构设计
- ✅ 规划开发阶段和时间节点
- ✅ 估算资源需求和成本
- ✅ 识别技术风险和应对措施

**使用说明**：
- 文档中的时间线为相对时间（Week 1-2, Month 1-2等）
- 实际日历时间取决于项目启动日期
- 所有代码示例仅供参考，实际开发中可能调整
- 成本估算基于当前市场情况，需定期更新

**本文档将持续更新，根据实际开发进度调整**

---

编写日期: 2024年11月  
最后更新: 2024年11月  
下次更新: 项目启动时
